type: google.api.Service
config_version: 3

http:
  rules:
    - selector: blockscout.stats.v1.Stats.GetStats
      get: /api/v1/stats

    - selector: blockscout.stats.v1.Health.Check
      get: /health
