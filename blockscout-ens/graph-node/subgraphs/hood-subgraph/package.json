{
  "name": "hood-subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create --node http://localhost:8020/ hood-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ hood-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 hood-subgraph",
    "test": "graph test"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.67.2",
    "@graphprotocol/graph-ts": "^0.31.0"
  }
}
