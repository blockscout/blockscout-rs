[package]
name = "sig-provider-extension"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1"
sig-provider-proto = { git = "https://github.com/blockscout/blockscout-rs" }
smart-contract-verifier = { path = "../smart-contract-verifier" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_with = "2"
tonic = "0.8"
tokio = "1"
http = "0.2"
