# Project Context

TODO: Add summarized tech info from Notion

Add Notion MCP to .mcp.json for team-wide access:

  {
    "mcpServers": {
      "notion": {
        "type": "url",
        "url": "https://mcp.notion.com/mcp"
      }
    }
  }

Each developer authenticates via OAuth on first use.
